export default function PayCancel() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-8 gap-3">
      <div className="text-3xl font-semibold">Payment cancelled</div>
      <p className="text-muted-foreground max-w-md text-center">
        No worries — give us a call back if you'd like to try again.
      </p>
    </main>
  );
}
